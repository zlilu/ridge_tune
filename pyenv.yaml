# Python version required to run the project.
python: "3.11.3"
# Dependencies required to build packages. This field is optional.
build_dependencies:
  - pip
  - setuptools
  - wheel==0.37.1
# Dependencies required to run the project.
dependencies:
  - scikit-learn==1.6.1
  - joblib==1.4.2
  - PyYAML>=6.0 # added 10.14 after fetch master
  - pandas>=2.2 # added 10.14
